{
  "$schema": "https://raw.githubusercontent.com/epoberezkin/ajv/master/lib/refs/json-schema-v5.json#",
  "id": "iht-paper",
  "type": "object",
  "properties": {},
  "switch": [
    {
      "if": {"properties": { "form": { "enum": [""] } }},
      "then": {"required": ["form"]},
      "continue": false
    },
    {
      "if": { "properties": { "form": { "enum": ["IHT205"] } } },
      "then": {
        "properties": {
          "grossIHT205": {
            "type": "string",
            "minLength": 1,
            "maxLength": 13
          },
          "netIHT205": {
            "type": "string",
            "minLength": 1,
            "maxLength": 13
          }
      },
        "required": ["grossIHT205", "netIHT205"]
      },
      "continue": false
    },
    {
      "if": { "properties": { "form": { "enum": ["IHT207"] } } },
      "then": {
        "properties": {
          "grossIHT207": {
            "type": "string",
            "minLength": 1,
            "maxLength": 13
          },
          "netIHT207": {
            "type": "string",
            "minLength": 1,
            "maxLength": 13
          }
      },
        "required": ["grossIHT207", "netIHT207"]
      },
      "continue": false
    },
    {
      "if": { "properties": { "form": { "enum": ["IHT400421"] } } },
      "then": {
        "properties": {
          "grossIHT400421": {
            "type": "string",
            "minLength": 1,
            "maxLength": 13
          },
          "netIHT400421": {
            "type": "string",
            "minLength": 1,
            "maxLength": 13
          }
      },
        "required": ["grossIHT400421", "netIHT400421"]
      },
      "continue": false
    }
  ]
}
