{
  "$schema": "https://raw.githubusercontent.com/epoberezkin/ajv/master/lib/refs/json-schema-v5.json#",
  "id": "applicant-alias-reason",
  "type": "object",
  "properties": {},
  "switch": [
    {
      "if": {"properties": { "aliasReason": { "enum": [""] } }},
      "then": {"required": ["aliasReason"]},
      "continue": false
    },
    {
      "if": { "properties": { "aliasReason": { "enum": ["Other"] } } },
      "then": {
        "properties": {
          "otherReason": {
            "type": "string",
            "minLength": 1,
            "maxLength": 50
          }
        },
        "required": ["otherReason"]
      },
      "continue": false
    }
  ]
}
