{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "applicant-name",
  "type": "object",
  "properties": {
    "firstName": {
      "type": "string",
      "pattern": "^[^<>]+$"
    },
    "lastName": {
      "type": "string",
      "pattern": "^[^<>]+$"
    }
  },
  "required": [
    "lastName",
    "firstName"
  ]
}
