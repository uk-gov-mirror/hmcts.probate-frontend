{{checkanswer(
    items = [
        {
            question: content.IhtMethod.question,
            answer: content.IhtMethod[fields.iht.method.value],
            visible: fields.iht.method.value and fields.summary.exceptedEstateDodAfterThreshold.value == "false"
        }
    ],
    url = content.IhtMethod.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{% set selectedOption = fields.iht.form.value | replace("option", "") %}

{{checkanswer(
    items = [
        {
            question: content.IhtPaper.question,
            answer:  content.IhtPaper["option" + selectedOption],
            visible: fields.iht.method.value == "optionPaper" and fields.summary.exceptedEstateDodAfterThreshold.value == "false"
        },
        {
            question: content.IhtValue.grossValue,
            answer: fields.iht["grossValueField" + selectedOption].value,
            visible: fields.iht.method.value == "optionPaper" and fields.summary.exceptedEstateDodAfterThreshold.value == "false"
        },
        {
            question: content.IhtValue.netValue,
            answer: fields.iht["netValueField" + selectedOption].value,
            visible: fields.iht.method.value == "optionPaper" and fields.summary.exceptedEstateDodAfterThreshold.value == "false"
        }
    ],
    url = content.IhtPaper.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: content.IhtIdentifier.question,
            answer: fields.iht.identifier.value,
            visible: fields.iht.method.value == "optionOnline" and fields.summary.exceptedEstateDodAfterThreshold.value == "false"
        }
    ],
    url = content.IhtIdentifier.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: content.ValueAssetsOutside.question,
            answer: fields.iht.netValueAssetsOutsideField.value,
            visible: fields.summary.caseType.value == "intestacy" and fields.iht.assetsOutside.value == "optionYes"
        }
    ],
    url = content.ValueAssetsOutside.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: content.CalcCheck.question,
            answer: content.CalcCheck[fields.iht.calcCheckCompleted.value],
            visible: fields.iht.calcCheckCompleted.value and fields.summary.exceptedEstateDodAfterThreshold.value == "true"
        }
    ],
    url = content.CalcCheck.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}


{{checkanswer(
    items = [
        {
            question: fields.summary.deceasedHadLateSpouseOrCivilPartnerQuestion.value,
            answer: content.DeceasedHadLateSpouseOrCivilPartner[fields.iht.deceasedHadLateSpouseOrCivilPartner.value],
            visible: fields.iht.deceasedHadLateSpouseOrCivilPartner.value and fields.summary.exceptedEstateDodAfterThreshold.value == "true" and fields.iht.estateValueCompleted.value == "optionNo" and fields.summary.withinNetQualifyingRange.value == "true"
        }
    ],
    url = content.DeceasedHadLateSpouseOrCivilPartner.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: fields.summary.ihtUnusedAllowanceClaimedQuestion.value,
            answer: content.IhtUnusedAllowanceClaimed[fields.iht.unusedAllowanceClaimed.value],
            visible: fields.iht.unusedAllowanceClaimed.value and fields.summary.exceptedEstateDodAfterThreshold.value == "true" and fields.iht.estateValueCompleted.value == "optionNo" and fields.iht.deceasedHadLateSpouseOrCivilPartner.value == "optionYes" and fields.summary.withinNetQualifyingRange.value == "true"
        }
    ],
    url = content.IhtUnusedAllowanceClaimed.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: content.NewSubmittedToHmrc.question,
            answer: content.NewSubmittedToHmrc[fields.iht.estateValueCompleted.value],
            visible: fields.iht.estateValueCompleted.value and fields.summary.exceptedEstateDodAfterThreshold.value == "true" and fields.iht.estateValueCompleted.value == "optionYes" and fields.iht.ihtFormEstateId.value == 'optionIHT207'
        }
    ],
    url = content.NewSubmittedToHmrc.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: content.NewSubmittedToHmrc.question,
            answer: content.NewSubmittedToHmrc[fields.iht.estateValueCompleted.value],
            visible: fields.iht.estateValueCompleted.value and fields.summary.exceptedEstateDodAfterThreshold.value == "true" and fields.iht.estateValueCompleted.value == "optionYes" and fields.iht.ihtFormEstateId.value == 'optionIHT400421'
        }
    ],
    url = content.NewSubmittedToHmrc.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: content.NewSubmittedToHmrc.question,
            answer: content.NewSubmittedToHmrc[fields.iht.estateValueCompleted.value],
            visible: fields.iht.estateValueCompleted.value and fields.summary.exceptedEstateDodAfterThreshold.value == "true" and fields.iht.estateValueCompleted.value == "optionYes" and fields.iht.ihtFormEstateId.value == 'optionIHT400'
        }
    ],
    url = content.NewSubmittedToHmrc.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: content.UniqueProbateCode.title,
            answer: fields.iht.uniqueProbateCodeId.value,
            visible: fields.summary.exceptedEstateDodAfterThreshold.value == "true" and fields.iht.ihtFormEstateId.value == 'optionIHT400' and fields.iht.hmrcLetterId.value == 'optionYes' and fields.iht.uniqueProbateCodeId.value !=''
        }
    ],
    url = content.UniqueProbateCode.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: content.IhtEstateForm.question,
            answer: content.IhtEstateForm[fields.iht.ihtFormId.value],
            visible: fields.summary.exceptedEstateDodAfterThreshold.value == "false" and fields.iht.ihtFormId.value == 'optionIHT205' and fields.iht.method.value == null
        }
    ],
    url = content.IhtEstateForm.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: content.IhtEstateForm.question,
            answer: content.IhtEstateForm[fields.iht.ihtFormId.value],
            visible: fields.summary.exceptedEstateDodAfterThreshold.value == "false" and fields.iht.ihtFormId.value == 'optionIHT400'
        }
    ],
    url = content.IhtEstateForm.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: content.IhtEstateForm.question,
            answer: content.IhtEstateForm[fields.iht.ihtFormId.value],
            visible: fields.summary.exceptedEstateDodAfterThreshold.value == "false" and fields.iht.ihtFormId.value == 'optionIHT400421' and fields.iht.method.value == null
        }
    ],
    url = content.IhtEstateForm.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}


{{checkanswer(
    items = [
        {
            question: content.UniqueProbateCode.title,
            answer: fields.iht.uniqueProbateCodeId.value,
            visible: fields.summary.exceptedEstateDodAfterThreshold.value == "false" and fields.iht.ihtFormId.value == 'optionIHT400' and fields.iht.hmrcLetterId.value == 'optionYes' and fields.iht.uniqueProbateCodeId.value !=''
        }
    ],
    url = content.UniqueProbateCode.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: content.IhtValue.grossValue,
            answer: fields.iht.grossValueField.value,
            visible: fields.summary.exceptedEstateDodAfterThreshold.value == "false" and fields.iht.method.value == "optionOnline"
        },
        {
            question: content.IhtValue.netValue,
            answer: fields.iht.netValueField.value,
            visible: fields.summary.exceptedEstateDodAfterThreshold.value == "false" and fields.iht.method.value == "optionOnline"
        }
    ],
    url = content.IhtValue.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: content.ProbateEstateValues.grossValueSummary,
            answer: fields.iht.grossValueField.value,
            visible: fields.summary.exceptedEstateDodAfterThreshold.value == "false" and fields.iht.method.value == null
        },
        {
            question: content.ProbateEstateValues.netValueSummary,
            answer: fields.iht.netValueField.value,
            visible: fields.summary.exceptedEstateDodAfterThreshold.value == "false" and fields.iht.method.value == null
        }
    ],
    url = content.ProbateEstateValues.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}


{{checkanswer(
    items = [
        {
            question: content.NewSubmittedToHmrc.question,
            answer: 'No forms required',
            visible: fields.iht.estateValueCompleted.value and fields.summary.exceptedEstateDodAfterThreshold.value == "true" and fields.iht.estateValueCompleted.value == "optionNo"
        }
    ],
    url = content.NewSubmittedToHmrc.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: content.IhtEstateValues.estateGrossValue + content.IhtEstateValues.forEstateIht,
            answer: fields.iht.estateGrossValueField.value,
            visible: fields.iht.estateValueCompleted.value == "optionNo" and fields.summary.exceptedEstateDodAfterThreshold.value == "true"
        },
        {
            question: content.IhtEstateValues.estateNetValue + content.IhtEstateValues.forEstateIht,
            answer: fields.iht.estateNetValueField.value,
            visible: fields.iht.estateValueCompleted.value == "optionNo" and fields.summary.exceptedEstateDodAfterThreshold.value  == "true"
        },
        {
            question: content.IhtEstateValues.estateNetQualifyingValue + " in " +content.IhtEstateValues.poundSymbol,
            answer: fields.iht.estateNetQualifyingValueField.value,
            visible: fields.iht.estateValueCompleted.value == "optionNo" and fields.summary.exceptedEstateDodAfterThreshold.value == "true"
        }
    ],
    url = content.IhtEstateValues.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: content.ProbateEstateValues.grossValueSummary,
            answer: fields.iht.grossValueField.value,
            visible: fields.iht.estateValueCompleted.value and fields.summary.exceptedEstateDodAfterThreshold.value == "true"
        },
        {
            question: content.ProbateEstateValues.netValueSummary,
            answer: fields.iht.netValueField.value,
            visible: fields.iht.estateValueCompleted.value and fields.summary.exceptedEstateDodAfterThreshold.value == "true"
        }
    ],
    url = content.ProbateEstateValues.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}
