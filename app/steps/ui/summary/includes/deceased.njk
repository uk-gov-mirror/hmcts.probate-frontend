{{checkanswer(
    items = [
        {
            question: content.BilingualGOP.question,
            answer: content.BilingualGOP[fields.language.bilingual.value],
            visible: true
        }
    ],
    url = content.BilingualGOP.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: content.DeceasedName.firstName,
            answer: fields.deceased.firstName.value,
            visible: true
        },
        {
            question: content.DeceasedName.lastName,
            answer: fields.deceased.lastName.value,
            visible: true
        }
    ],
    url = content.DeceasedName.url if fields.summary.caseType.value == "gop" else content.DeceasedDetails.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: fields.summary.deceasedAliasQuestion.value,
            answer: content.DeceasedAlias[fields.deceased.alias.value],
            visible: true
        }
    ],
    url = content.DeceasedAlias.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{% if fields.deceased.otherNames.value.length > 0 %}
    <div class="govuk-summary-list__row">
        <dt class="govuk-summary-list__key">{{ content.Summary.otherNamesLabel | safe }}</dt>
        <dd class="govuk-summary-list__value">
            {% for index, otherName in fields.deceased.otherNames.value %}
                <div class="govuk-summary-list__row">
                    {{ otherName.firstName.value | safe }} {{ otherName.lastName.value | safe }}
                </div>
            {% endfor %}
        </dd>
        <dd class="govuk-summary-list__actions">
            {% if fields.summary.alreadyDeclared.value == "false" %}
                <a href="{{ content.DeceasedOtherNames.url }}" class="govuk-link">{{ common.change | safe }}</a>
            {%endif%}
        </dd>
    </div>
{% endif %}

{{checkanswer(
    items = [
        {
            question: fields.summary.deceasedMarriedQuestion.value,
            answer: content.DeceasedMarried[fields.deceased.married.value],
            visible: fields.summary.caseType.value == "gop"
        }
    ],
    url = content.DeceasedMarried.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: content.DeceasedDob.question,
            answer: fields.deceased["dob-formattedDate"].value,
            visible: true
        }
    ],
    url = content.DeceasedDob.url if fields.summary.caseType.value == "gop" else content.DeceasedDetails.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: content.DeceasedDod.question,
            answer: fields.deceased["dod-formattedDate"].value,
            visible: true
        }
    ],
    url = content.DeceasedDod.url if fields.summary.caseType.value == "gop" else content.DeceasedDetails.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: fields.summary.deceasedMaritalStatusQuestion.value,
            answer: content.DeceasedMaritalStatus[fields.deceased.maritalStatus.value],
            visible: fields.summary.caseType.value == "intestacy"
        }
    ],
    url = content.DeceasedMaritalStatus.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: fields.summary.deceasedDivorcePlaceQuestion.value,
            answer: content.DivorcePlace[fields.deceased.divorcePlace.value],
            visible:
                fields.summary.caseType.value == "intestacy"
                and
                (
                    fields.deceased.maritalStatus.value == "optionDivorced"
                    or
                    fields.deceased.maritalStatus.value == "optionSeparated"
                )
        }
    ],
    url = content.DivorcePlace.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: content.DeceasedAddress.question,
            answer: fields.deceased.address.value.formattedAddress,
            visible: true
        }
    ],
    url = content.DeceasedAddress.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: fields.summary.diedEnglandOrWalesQuestion.value,
            answer: content.DiedEnglandOrWales[fields.deceased.diedEngOrWales.value],
            visible: fields.deceased.diedEngOrWales.value
        }
    ],
    url = content.DiedEnglandOrWales.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: content.DeathCertificateInterim.question,
            answer: content.DeathCertificateInterim[fields.deceased.deathCertificate.value],
            visible: fields.deceased.deathCertificate.value
        }
    ],
    url = content.DeathCertificateInterim.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: content.EnglishForeignDeathCert.question,
            answer: content.EnglishForeignDeathCert[fields.deceased.englishForeignDeathCert.value],
            visible: fields.deceased.englishForeignDeathCert.value
        }
    ],
    url = content.EnglishForeignDeathCert.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}

{{checkanswer(
    items = [
        {
            question: content.ForeignDeathCertTranslation.question,
            answer: content.ForeignDeathCertTranslation[fields.deceased.foreignDeathCertTranslation.value],
            visible: fields.deceased.foreignDeathCertTranslation.value
        }
    ],
    url = content.ForeignDeathCertTranslation.url,
    common = common,
    alreadyDeclared = fields.summary.alreadyDeclared.value
)}}
