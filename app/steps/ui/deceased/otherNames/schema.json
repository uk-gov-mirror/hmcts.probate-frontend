{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "other-names",
  "type": "object",
  "properties": {
    "firstName": {
      "type": "string",
      "pattern": "^[^<>]+$"
    },
    "lastName": {
      "type": "string",
      "pattern": "^[^<>]+$"
    }
  },
  "required": [
    "firstName",
    "lastName"
  ]
}
